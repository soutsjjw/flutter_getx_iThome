name: flutter_getx_ithome
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  get:
  equatable: ^2.0.3
  lottie: ^1.2.1
  carousel_slider: ^4.0.0
  shared_preferences: ^2.0.8
  image_picker: ^0.8.4+4
  image_cropper: ^1.4.1
  gallery_saver: ^2.3.2
  flutter_slidable: ^0.6.0
  fluttertoast: ^8.0.8
  cached_network_image: ^3.1.0
  qr_flutter: ^4.0.0
  qr_code_scanner: ^0.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  assets:
    - assets/
    - assets/lottie/

  uses-material-design: true